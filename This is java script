javascript// A simple function to add two numbers
function add(a, b) {
  return a + b;
}

// A simple function to subtract two numbers
function subtract(a, b) {
  return a - b;
}

module.exports = { add, subtract };
